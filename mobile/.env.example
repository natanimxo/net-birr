# Local dev: point this at your machine's LAN IP (not localhost/127.0.0.1) so a
# physical phone on the same WiFi can reach the FastAPI backend.
# Find it with `ipconfig` (Windows) - look for IPv4 Address under your WiFi adapter.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
